package medgemma import ( "fmt" "gentle-care/pkg/triage" ) type Orchestrator struct { isOnline bool } func NewOrchestrator(isOnline bool) \*Orchestrator { return &amp;Orchestrator{isOnline: isOnline} } // InferTriage executa la inferencia acotada o conmuta al determinismo duro en Go (Fail-Close) func (o \*Orchestrator) InferTriage(symptoms []string, snomedCode string) (triage.Assessment, string, string) { if o.isOnline { // Simulación de respuesta acotada de MedGemma en Vertex AI assessment, receipt := triage.Evaluate(symptoms, false, snomedCode) return assessment, receipt, "VERTEX\_AI\_MEDGEMMA\_ONLINE" } // Contingencia Offline: Triage Determinista Local en Pixel 11 Pro assessment, receipt := triage.Evaluate(symptoms, false, snomedCode) return assessment, receipt, "LOCAL\_EDGE\_DETERMINISTIC\_FAIL\_CLOSE" }
